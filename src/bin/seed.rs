// MongoDB seed removed after PostgreSQL migration.
// Data seeding is now handled via SQL migration files in migrations/.
fn main() {
    println!("Seed is a no-op after PostgreSQL migration. Use SQL migrations instead.");
}
